---
import BaseLayout from '@/layouts/BaseLayout.astro';

const PAGE_ID = 'TylerTaylorComposer';
const APP_ACCESS_TOKEN = import.meta.env.SECRET_FB_ACCESS_TOKEN;

// const response = await fetch(
//     `https://graph.facebook.com/v21.0/${PAGE_ID}/posts?access_token=${APP_ACCESS_TOKEN}&fields=message,created_time,full_picture,permalink_url`,
// );
// const stuff = await response.json();

const response = await fetch(
    `https://graph.facebook.com/v18.0/oembed_page?url=https://www.facebook.com/TylerTaylorComposer&access_token=${import.meta.env.SECRET_FB_ACCESS_TOKEN}`,
);
const embedData = await response.json();

console.log(embedData);
---

<BaseLayout title="News" description="Latest news from Tyler Taylor"> tempoar </BaseLayout>
