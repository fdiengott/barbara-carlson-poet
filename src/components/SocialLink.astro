---
// const { href, ariaLabel, icon: Icon } = Astro.props;
const { href, ariaLabel } = Astro.props;
---

{
    href && (
        <li class="inline-block">
            <a aria-label={ariaLabel} href={href} target="_blank" rel="noopener noreferrer nofollow">
                {/*  TODO:  */}
                {/* <Icon size="1.2rem" /> */}
            </a>
        </li>
    )
}
